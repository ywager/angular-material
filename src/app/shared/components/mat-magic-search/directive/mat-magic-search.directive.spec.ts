import { MatMagicSearchDirective } from './mat-magic-search.directive';

describe('MatMagicSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new MatMagicSearchDirective();
    expect(directive).toBeTruthy();
  });
});
